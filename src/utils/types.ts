export type CardType = {
  id: number;
  heading: string;
  title: string;
  subtitle: string;
  icon: string;
};

export type CarouselType = {
  avatar: string;
  title: string;
  subtitle: string;
  client: string;
};
